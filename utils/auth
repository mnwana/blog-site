// const withAuth = (req, res, next) => {
//   if (!req.session.user_id) {
//     res.redirect('/login');
//   } else {
//     next();
//   }
// };

const withAuth = (req, res, next) => {
      next();
  };

module.exports = withAuth;
